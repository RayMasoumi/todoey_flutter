import 'package:flutter/material.dart';

const Color kMainOrange = Color(0xFFfa8100);
const kRadius = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.only(
    topRight: Radius.circular(30.0),
    topLeft: Radius.circular(30.0),
  ),
);
